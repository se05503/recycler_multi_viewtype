package com.example.standard_assignment_4.presentation

enum class MultiViewEnum(val viewType: Int) {
    // multi view type의 경우 enum class를 사용함?
    BLUE(0),
    LIGHTBLUE(1),
    ORANGE(2)
    // default : 0
}